{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "NODE_OPTIONS='--openssl-legacy-provider' dotnet fable watch src --run webpack-dev-server",
    "build": "NODE_OPTIONS='--openssl-legacy-provider' dotnet fable src && webpack"
  },
  "devDependencies": {
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.0",
    "webpack-dev-server": "^3.11.2"
  },
  "version": "1.0.0",
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
